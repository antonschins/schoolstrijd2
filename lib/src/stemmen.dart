import 'package:flutter/material.dart';

class Stemmen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stemmen'),
      ),
      body: Center(
        child: Text('STEMMEN'),
      ),
    );
  }
}